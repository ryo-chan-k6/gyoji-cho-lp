# Codex互換定義

GYOJI-CHOの主実行環境は **Cursor** です。役割定義の正本は [`.cursor/agents/`](../.cursor/agents/) に置きます。

このディレクトリの `agents/*.toml` は、Codexデスクトップを補助利用する場合の互換コピーです。役割・権限・停止条件を変える場合は、先に `.cursor/agents/` を更新し、同じ内容をここに反映してください。
